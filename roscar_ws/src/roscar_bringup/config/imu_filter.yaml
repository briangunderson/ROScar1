imu_filter:
  ros__parameters:
    fixed_frame: "odom"
    use_mag: false
    publish_tf: false
    world_frame: "enu"
    orientation_stddev: 0.05
    # Gain: lower = trust gyro more, less accel noise sensitivity
    # 0.1 is default; 0.01 is conservative.  Start with default.
    gain: 0.1
