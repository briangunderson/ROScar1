# Working-State Snapshot — 2026-04-30

User asked: *"things are working great! Can we save a full, complete snapshot of everything?"*

This document is the index for that snapshot. The snapshot covers three layers:

1. **Code** — git state on master + the open fix PR
2. **Runtime state** — Pi's `~/maps/` and learned markers
3. **Verification** — what was working when the snapshot was taken

---

## 1. Code state

### Master tip
- `144bdb0` — save_map: skip empty-sidecar write when no markers + no prior sidecar (PR #25)

### Pending fix that must merge before tagging
- **PR #26** `fix/setmode-use-depth-field` — adds `bool use_depth` to `SetMode.srv` which has been silently missing from master. The Pi has been working only because its physical (non-symlinked) copy of `roscar_interfaces` was deployed earlier from `feat/d435i-depth-camera` and already has the field. A clean clone of master would crash on `request.use_depth` AttributeError at first dashboard mode-switch.

### Tag plan
After PR #26 merges:
```
git fetch origin master
git tag -a working-slam-nav-2026-04-30 -m "Working SLAM + Nav2 with dashboard, live DWB tuning, per-map marker sidecars, recovery page Pull/Rebuild buttons" origin/master
git push origin working-slam-nav-2026-04-30
```

### Recent shipped work (chronological)
- **#15** chassis v2 deploy + Pi exec-bit fixes
- **#16** depth overlay default OFF
- **#17** AIO dashboard always passes `use_depth: true`
- **#18** nav-goal-mode no longer clobbered by INIT pose; periodic mode sync
- **#19** recovery page: Pull-from-GitHub button
- **#20** revert `set_initial_pose` to true (false breaks lifecycle bringup)
- **#21** route nav goals through topic shim instead of broken `ROSLIB.ActionClient`
- **#22** bias DWB toward forward driving + live tuning sliders
- **#23** recovery page: Rebuild + Restart (handles new files in `web/`)
- **#24** gate nav-tuning param pushes on nav mode being active
- **#25** save_map: skip empty-sidecar write when no markers + no prior sidecar

### Diverged branch (intentionally not merged)
- `feat/d435i-depth-camera` — 17 commits ahead of master, mostly D435i tuning. Not merged because its launch-file changes overlap with master's slam_nav refactor; cherry-picks have been lifting individual fixes (most recently the `use_depth` field via PR #26).

---

## 2. Runtime state backups

Local copies live in `snapshots/` (gitignored). These are fresh as of 2026-04-30 16:15 EDT.

| File | Size | Source on Pi | Contents |
|------|------|--------------|----------|
| `ROScar1.predeploy.20260430-160949.tgz` | 10.8 MB | `~/ROScar1.predeploy.*.tgz` | Pi's `~/ROScar1` tree (excludes `.git`, `roscar_ws/build`, `install`, `log`). Captures any uncommitted scp-deployed CRLF drift before reset. |
| `maps-snapshot-20260430-161511.tgz` | 56 KB | `~/maps-snapshot-*.tgz` | All 23 saved SLAM maps + their `.markers.yaml` sidecars + the legacy `learned_markers.yaml.bak.20260428-155837`. |

### Pi maps inventory
23 maps total in `~/maps/`. Recent (Apr 28-30): `apr28*`, `bb*`, `bbb*`, `bgb`, `eee`, `ewe`, `ff`, `niceapr28`, `qe`, `wewe`, `a291`. Older (Mar): `mar16*`, `one`, `b1`, `g`, `sn`.

`niceapr28` and `apr28-5` carry real marker sidecars from when landmark_localizer was active in slam_nav mode (since reverted — the localizer is now nav-only).

---

## 3. Verification — what works as of this snapshot

### Pi services (all enabled, auto-start on boot)
- `roscar-web.service` — rosbridge + web_video_server + launch_manager + http_server
- `roscar-recovery.service` — port 9999 admin page (Start/Stop/Restart/Pull/Rebuild)

### Verified working flows
- **Dashboard** at `http://192.168.1.170:8888/aio.html`
- **Mode switching** via dashboard (idle ↔ teleop ↔ slam ↔ navigation ↔ slam_nav) all pass `use_depth: true`
- **SLAM mapping** — slam_toolbox publishing /map, walls staying straight (time-triggered scan matching, host-side mecanum FK in driver)
- **Map save** with per-map marker sidecar (silent when no markers, no log spam)
- **Nav2 click-to-navigate** via `/web/nav_goal` topic shim (replaces broken roslibjs `ROSLIB.ActionClient`)
- **Nav cancel** via `/web/cancel_nav_goal`
- **Initial pose** via dashboard map panel — INIT button + click-and-drag for explicit yaw
- **Live DWB tuning** — sliders for max forward / max strafe / vy_samples / Twirling.scale, persisted in localStorage, gated on nav mode being active
- **Recovery page** at `http://192.168.1.170:9999/` — Pull from GitHub + Rebuild + Restart buttons all functional
- **slam→nav pose carryover** — final TF lookup → /initialpose at t+3/6/10s after mode change

### Pi runtime topology
- `~/ROScar1` is the source-of-truth git clone
- `~/roscar_ws/src/*` is mostly symlinks to `~/ROScar1/roscar_ws/src/*` — except `roscar_interfaces` and `sllidar_ros2` which are physical copies
- That non-symlinked `roscar_interfaces` is exactly why master's missing `use_depth` field went unnoticed

### Pi-vs-master content drift (verified at snapshot time)
All 9 modified files on the Pi diff cleanly to master under `--ignore-all-space` (CRLF only, content-equivalent). Hard reset to master is safe IF PR #26 merges first — without that, hard reset would strip the working `use_depth` field.

---

## Restore procedure

### Restore master to working state
```bash
git fetch origin
git checkout working-slam-nav-2026-04-30
```

### Restore Pi maps
```bash
scp snapshots/maps-snapshot-20260430-161511.tgz brian@192.168.1.170:/tmp/
ssh brian@192.168.1.170 "cd ~ && tar xzf /tmp/maps-snapshot-20260430-161511.tgz"
```

### Restore Pi tree (full nuclear option)
```bash
scp snapshots/ROScar1.predeploy.20260430-160949.tgz brian@192.168.1.170:/tmp/
ssh brian@192.168.1.170 "cd ~ && mv ROScar1 ROScar1.broken && tar xzf /tmp/ROScar1.predeploy.20260430-160949.tgz"
# Then re-clone .git from GitHub (the tarball excludes it)
```

---

## Pending follow-ups
- [ ] Merge PR #26 (`fix/setmode-use-depth-field`)
- [ ] Hard-reset Pi to master once #26 lands; clean these stale untracked junk files:
  - `roscar_ws/src/roscar_description/roscar.urdf.xacro` (real one is under `urdf/`)
  - `roscar_ws/src/roscar_web/web/aio-camera.js` (real one is under `web/js/`)
  - `roscar_ws/src/roscar_web/web/aio-cv.js` (real one is under `web/js/`)
- [ ] Tag `working-slam-nav-2026-04-30` on master and push
- [ ] After tag pushed: `colcon build --symlink-install --packages-select roscar_interfaces roscar_web` on Pi, verify dashboard mode switch logs `use_depth=true`
